var React = require('react');

var errorMsg = React.createClass({
    render: function() {
        return (
            <html>
            <body>
            ERROR!
            </body>
            </html>
        )
    }
});

module.exports = errorMsg;